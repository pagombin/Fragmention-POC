# Troubleshooting

_Populated from Phase 17 onward._

Common failure modes and resolutions will be catalogued here as the
application matures. Until then, consult `journalctl -u mfpoc` for structured
logs and `/health` / `/ready` for liveness.
