# POC methodology

_Populated in Phase 19. Covers:_

- How to design a valid POC comparing `compact` vs. initial sync.
- Why pre-fragmented baselines matter and how to establish them.
- How to interpret reports where `compact` reclaims less than expected.
- The role of update operations in producing realistic fragmentation.
- ObjectID vs. UUID `_id` tradeoffs for fragmentation patterns.
- Compression codec impact (snappy vs. zstd) on reclaim perception.
